# maps placeholder

This folder stores navigation map assets for SemuBot.

Current status:
- No production map is committed yet.
- Navigation readiness is currently mock-first and odometry/TF focused.

When maps are added, include:
- map YAML file
- image file (`.pgm` or `.png`)
- metadata notes (environment, date, robot configuration)
